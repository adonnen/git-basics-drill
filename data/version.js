/* =============================================================================
   version.js — what this copy is.

   The committed value below is the placeholder every local copy shows. The
   Pages workflow overwrites this file at deploy time with the output of
   `git describe --tags`, so the published site reports the release tag it
   was actually built from — see .github/workflows/static.yml.

   Do not bump this by hand. The release tag is the single source of the
   version; there is nothing here to keep in step with it.
   ============================================================================= */

const VERSION = "dev";
